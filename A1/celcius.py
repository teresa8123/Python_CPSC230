# Convert the temperature from celcius to feirenheight

temp_c = float(input('What is the temperature in celcius:'))
temp_f = (float(temp_c) * 1.8) + 32

print('Temperature in feirenheight is:', int(temp_f))
